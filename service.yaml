apiVersion: v1
kind: Service
metadata:
  name: car-app-service
spec:
  type: NodePort
  selector:
    app: car-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007
