apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: car-app
  template:
    metadata:
      labels:
        app: car-app
    spec:
      containers:
      - name: car-app
        image: car-app:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
